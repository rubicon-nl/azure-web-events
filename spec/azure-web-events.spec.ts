import { AzureWebEvents } from "../src"

describe('Azure web events test', async () => {
    const azureWebEvents: AzureWebEvents = new AzureWebEvents();

    it('Initialize azure web events', async () => {
    });
});